server {

    listen      80;
    server_name  _;

    location /server_status {
        # allow 127.0.0.1;
        # deny all;
        stub_status;
    }

    location /{
        return 301 https://$host$request_uri;
    }
}

